#ifndef UTILS_H
#define UTILS_H
#include <string>
#include <iostream>
#include <vector>

namespace utils
{
    std::vector<std::string> split(const std::string &str, const std::string &delim);
}

#endif